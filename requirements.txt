# Core LangChain packages
langchain==1.1.0
langgraph==1.0.4
langchain-community==0.4.1  
langchain-google-genai==3.2.0 
langchain-huggingface==1.1.0  
langchain-qdrant==1.1.0

# Vector DB
qdrant-client==1.16.1 

# Database
psycopg2-binary==2.9.11 
sqlalchemy==2.0.35  # Keep at 2.0.35 (langchain-community requires <2.0.36)

# Security & LLM Guard
llm-guard==0.3.16 
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.5.0

# ML & Embeddings
sentence-transformers==3.3.1  # Keep current (5.1.2 is major update)
numpy==1.26.4  # MUST keep <2.0 for compatibility
tiktoken==0.12.0  # Token counting for context management

# Web Framework
fastapi==0.123.0
uvicorn==0.38.0
streamlit==1.51.0

# HTTP & Utilities
httpx==0.28.1
requests==2.32.5
python-multipart==0.0.20
python-dotenv==1.2.1

# Document Processing
pypdf==5.1.0  # Keep current (6.4.0 is major update)
python-docx==1.1.0
beautifulsoup4==4.12.3
openpyxl==3.1.2

# Search
google-search-results==2.4.2

# Logging
python-json-logger==2.0.7  # Keep current (4.0.0 is major update)

# Testing & Development
pytest==8.3.3  # Keep current (9.0.1 is major update)
pytest-asyncio
ruff==0.14.7

# Data & Evaluation
ragas==0.2.9  # Keep current (0.3.9 requires datasets>=4.0.0)
datasets==3.1.0

# Cache
redis==5.2.0
